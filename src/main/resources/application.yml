spring:
  application:
    name: GesRDV
  datasource:
    url: jdbc:mysql://localhost:3306/GesRDV
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
      database-platform: org.hibernate.dialect.MySQLDialect
      hibernate:
        ddl-auto: update
      show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: "emailservice"
    password: "passwordmail"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
